# Example Nginx Reverse Proxy Configuration
# Add this to your main nginx server that will proxy to this project

server {
    listen 80;
    server_name your-domain.com;

    # Proxy to Project A
    location /projecta/ {
        proxy_pass http://projetoa:80/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # WebSocket support (if needed)
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }

    # Add other project locations here
    # location /projectb/ {
    #     proxy_pass http://projectb:80/;
    #     ...
    # }
}

# For Docker Compose network integration
# Make sure this nginx container is on the same network as projetoa
# Example docker-compose.yml for the reverse proxy:
#
# version: '3.8'
# services:
#   nginx-proxy:
#     image: nginx:alpine
#     ports:
#       - "80:80"
#       - "443:443"
#     volumes:
#       - ./nginx-reverse-proxy.conf:/etc/nginx/conf.d/default.conf
#     networks:
#       - app-network
# 
# networks:
#   app-network:
#     external: true
